kind: Deployment
apiVersion: apps/v1
metadata:
  name: gsv-deploy
spec:
  replicas: 3
  progressDeadlineSeconds: 600
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 50%
  template:
    spec:
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      containers:
        - name: gsv-deploy
          image: 'harbor.nuclearport.com/jackpotland/gsv:pro'
          imagePullPolicy: Always
          ports:
            - name: http-9091
              containerPort: 9091
              protocol: TCP
          env:
            - name: ip
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: env
              valueFrom:
                configMapKeyRef:
                  name: gsv-cm
                  key: env
            - name: ver
              valueFrom:
                configMapKeyRef:
                  name: gsv-cm
                  key: ver
          resources:
            limits:
              cpu: 100m
              memory: 100Mi
            requests:
              cpu: 50m
              memory: 50Mi
          lifecycle:
            preStop:
              exec:
                command:
                  - sleep
                  - '15'
          livenessProbe:
            httpGet:
              path: /heartbeat
              port: 9091
              scheme: HTTP
            initialDelaySeconds: 5
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /heartbeat
              port: 9091
              scheme: HTTP
            initialDelaySeconds: 1
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
      imagePullSecrets:
        - name: gsv-secret

