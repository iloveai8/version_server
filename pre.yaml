apiVersion: v1
kind: ConfigMap
metadata:
  data:
    env: pre
  labels:
    app: gsv
    env: pre
  name: gsv-config-pre-v
  namespace: jackpotland
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: gsv
    env: pre
  name: gsv-service-pre-v
  namespace: jackpotland
spec:
  ports:
  - name: http-19091
    port: 19091
    protocol: TCP
    targetPort: 9091
  selector:
    app: gsv
    env: pre
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gsv
    env: pre
  name: gsv-deployment-pre-v
  namespace: jackpotland
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: gsv
      env: pre
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gsv
        env: pre
    spec:
      containers:
      - env:
        - name: env
          valueFrom:
            configMapKeyRef:
              key: env
              name: gsv-config-pre-v
        - name: vsn
          valueFrom:
            configMapKeyRef:
              key: vsn
              name: gsv-config-pre-v
        image: harbor.nuclearport.com/jackpotland/game_slots_vsn:latest
        imagePullPolicy: Always
        livenessProbe:
          failureThreshold: 10
          httpGet:
            path: /heartbeat
            port: 9091
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        name: gsv-pod
        ports:
        - containerPort: 9091
          name: http-9091
          protocol: TCP
        readinessProbe:
          failureThreshold: 10
          httpGet:
            path: /heartbeat
            port: 9091
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        resources:
          limits:
            cpu: 50m
            memory: 50Mi
          requests:
            cpu: 10m
            memory: 25Mi
      imagePullSecrets:
      - name: jackpotland-image-repository-secret
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
